function add (a, b){
  return a + b;
}

function mult (a, b){
  return a * b;
}
 module.exports.add = add;
 module.exports.mult = mult;
 
/*
var maxCount = 10;
var a = 1;
start = new Date().getTime();
for (var i = 0; i < maxCount; i++) {

}
elapsed = new Date().getTime() - start;
assert(true, "Mesuared time: " + elapsed);

assert(a == 1, " a is not 1");

function assert(a, str) {
   if (a == true){
     return "ok";
   }else { return str;}
}
*/
